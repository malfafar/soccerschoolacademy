# json.array!(@statistics) do |statistic|
#   json.extract! statistic, :id, :first_yellow, :second_yellow, :red, :goals, :own_goals, :state, :called, :player_id, :match_id
#   json.url statistic_url(statistic, format: :json)
# end
